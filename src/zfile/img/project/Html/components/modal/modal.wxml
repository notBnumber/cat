<view class='mask' wx:if='{{show}}' bindtap='clickMask'>
    <view class='modal-content' style='height:{{height}};width:{{width}}' catchtap="stop">
        <scroll-view scroll-y class='main-content'>
            <slot></slot>
        </scroll-view>
    </view>
</view>
